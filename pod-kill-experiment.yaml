apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-example
  namespace: default
spec:
  action: pod-kill
  mode: all
  selector:
    labelSelectors:
      app: webapp
  scheduler:
    cron: "@every 5m" # Cada 5 minutos